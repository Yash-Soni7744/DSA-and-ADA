// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;

int main() {
    int matrix_no,arr[],rows, columns;
        
    std::cout<<"enter the no. of matrices to multiply: ";
    cin>>matrix_no;
    
    for(int i=1; i<matrix_no; i++){
        cout<<"enter the no. of rows for "<<i<<" matrix";
        cin>>rows;
        cout<<"enter the no. of columns for "<<i<<" matrix";
        cin>>columns;
        
        int A[rows][columns];
        arr.push(A);
    }
    
    for(int i=0; i<end(arr)-begin(arr); i++){
        
    }
    
    
    
    
